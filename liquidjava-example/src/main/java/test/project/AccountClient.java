package test.project;

// @StateSet({"active", "inactive"})
public class AccountClient {

  //	@StateRefinement(from="active(this)", to="active(this)")
  //	public boolean transfer(Account from, Account to, @Refinement("amount(from) >= value") int
  // value) {
  //		from.withdraw(value);
  //		to.add(value);
  //		return true;
  //	}

}
